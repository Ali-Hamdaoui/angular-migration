# Sandbox

Owns legacy sandbox-policy notes retained for compatibility while workspace,
command, and runtime-profile boundaries mature.

New mutation and execution behavior should live in the dedicated workspaces,
command execution, runtime profile, and policy modules. This module must not
become a parallel execution path.