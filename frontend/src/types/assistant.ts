export type AssistantEvidence = { artifact_id: string; checksum: string; label: string };
export type AssistantUsage = { input_tokens: number; output_tokens: number; total_tokens: number; estimated_input_cost: number; estimated_output_cost: number; estimated_total_cost: number };
export type AssistantOperationalStatistics = { command_totals_by_status: Record<string, number> | null; successful_commands: number | null; failed_commands: number | null; llm_calls_by_role: Record<string, number> | null; input_tokens: number | null; output_tokens: number | null; total_tokens: number | null; input_cost_usd: number | null; output_cost_usd: number | null; total_cost_usd: number | null; recorded_workflow_duration_seconds: number | null };
export type AssistantMessage = { message_id: string; message_order: number; conversation_id: string; run_id: string; role: "user" | "assistant"; answer: string; current_phase: string; current_stage: string; workflow_status: string; current_gate: string; current_blocker: string; next_permitted_action: string; workflow_state_version: number; stale: boolean; evidence_references: AssistantEvidence[]; proof_label: string; usage: AssistantUsage; response_status: string; failure_reason: string | null; operational_statistics?: AssistantOperationalStatistics | null };
export type AssistantHistory = { run_id: string; conversation_id: string; messages: AssistantMessage[] };
