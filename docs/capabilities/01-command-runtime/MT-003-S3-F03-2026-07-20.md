# MT-003 — S3-F03 live logs and reconnect evidence

Date: 2026-07-20  
Run: `manual-s3f03-20260720`  
Execution: `exec-06b19438e09d`  
Actor: `local-operator` for the browser; authenticated API replay was also verified with the scoped run actor.  
Command: `python --version`  
Correlation ID: `corr-manual-s3f03`

## Execution and durable order

The command completed successfully with exit code `0`, duration `105 ms`, and output `Python 3.14.6`. The durable workflow event sequence was:

1. `COMMAND_QUEUED`
2. `COMMAND_STARTED`
3. `COMMAND_OUTPUT_AVAILABLE` (stdout sequence `1`, correlation ID preserved)
4. `COMMAND_SUCCEEDED`

The stored-log summary reported one stdout chunk, zero stderr/system chunks, `finalized: true`, `last_sequence: 1`, and no truncation or redaction.

## Live stream, replay, and reconnect

- A fresh SSE request (`cursor=0`) returned `id: 1` `command_log`, then `log_checkpoint`, then `execution_complete`.
- Reconnecting with `Last-Event-ID: 1` returned only `execution_complete`; sequence `1` was not duplicated.
- `stream=stdout` returned the stdout chunk.
- Pagination with `limit=1` returned the chunk at offset `0`; offset `1` returned an empty page with `total: 1`.

## Artifact verification

All five registered artifacts were readable through the artifact API and were returned as immutable, non-redacted, non-truncated metadata. The output checksums were:

| Artifact | SHA-256 |
|---|---|
| `artifact-f9d35f2e5c94472fa34b36cb0aea86df` stdout | `sha256:2b095b05c4093ebca54b87bd2c6a6f557935e6b3f8f506a0c2e5e294a763f4ad` |
| `artifact-4b76fcce8c204bbf8aeebfad796b79ca` stderr | `sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855` |
| `artifact-08bb4c238cef4913a5249581b472d414` command log | `sha256:1140ae6e7007fa0480de850d5a5a7dcd32ea3b083c50a4cb705c15548e560c9a` |
| `artifact-72ffae6e90ce49beb2ae219fdbdb6ff9` result | `sha256:f092ebe16998af875f8ad1e8fb619228db3303bd53028cf8e7dc2eea35274978` |
| `artifact-80329f197f4344c9b0973ca687c50700` manifest | `sha256:8ee4a214beba9940c3e15abe9b9a61bbc29d2feb5840f0b4fa82580aa15e70b1` |

## Negative and security checks

- Unsupported `stream=debug` returned HTTP 422 with machine-readable `INVALID_LOG_STREAM`, allowed-stream details, and a correlation ID.
- Malformed `Last-Event-ID: nope` returned HTTP 422 with `INVALID_LAST_EVENT_ID` and a correlation ID.
- A nonexistent execution in the run scope returned `EXECUTION_NOT_FOUND` and a correlation ID.
- Automated async SSE coverage also verifies stream failure emits `LOG_STREAM_FAILED` with the execution correlation ID, and persistence redacts secret values.

## Browser result

The Next.js browser route was loaded against the real backend and rendered the authoritative run dashboard, the S3-F02 command-execution panel, all four durable workflow events, and the artifact evidence list. The frontend test suite covers the interactive pause/play, search, loading/empty, reconnect/recovery, correlation-ID, stored-log pagination, and artifact metadata behaviors; those tests passed in isolation and the full suite was rerun after the final changes. Browser automation was used to confirm the rendered dashboard and durable execution snapshot; the short `python --version` fixture finalized before a human pause gesture could affect consumption, so pause/play behavior is validated by the dedicated component tests rather than claimed as a timing-sensitive manual observation.

