# Hermes Launch Commands

Run Hermes from each assigned worktree. The full contract is in the goal folder.

## G01 — Governed Command Runtime

```bash
cd /home/ubuntu/amfa-worktrees/01-command-runtime
bash goals/scripts/check-goal-worktree.sh 01-command-runtime hermes/01-command-runtime
hermes
```

Submit:

```text
/goal Execute goals/01-command-runtime/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/01-command-runtime after branch_ready is fully green.
```

## G02 — Stage Workspace, G07, and Bootstrap

```bash
cd /home/ubuntu/amfa-worktrees/02-stage-workspace-bootstrap
bash goals/scripts/check-goal-worktree.sh 02-stage-workspace-bootstrap hermes/02-stage-workspace-bootstrap
hermes
```

Submit:

```text
/goal Execute goals/02-stage-workspace-bootstrap/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/02-stage-workspace-bootstrap after branch_ready is fully green.
```

## G03 — Exact Angular Transformation and G08

```bash
cd /home/ubuntu/amfa-worktrees/03-angular-transform-review
bash goals/scripts/check-goal-worktree.sh 03-angular-transform-review hermes/03-angular-transform-review
hermes
```

Submit:

```text
/goal Execute goals/03-angular-transform-review/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/03-angular-transform-review after branch_ready is fully green.
```

## G04 — Stage Validation, G09, G12, and Copy-Forward

```bash
cd /home/ubuntu/amfa-worktrees/04-stage-validation-seal
bash goals/scripts/check-goal-worktree.sh 04-stage-validation-seal hermes/04-stage-validation-seal
hermes
```

Submit:

```text
/goal Execute goals/04-stage-validation-seal/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/04-stage-validation-seal after branch_ready is fully green.
```

## G05 — Failure Evidence, C-Lite, and Repair Context

```bash
cd /home/ubuntu/amfa-worktrees/05-failure-diagnostics-context
bash goals/scripts/check-goal-worktree.sh 05-failure-diagnostics-context hermes/05-failure-diagnostics-context
hermes
```

Submit:

```text
/goal Execute goals/05-failure-diagnostics-context/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/05-failure-diagnostics-context after branch_ready is fully green.
```

## G06 — Repair Proposer, Reviewer, and G10

```bash
cd /home/ubuntu/amfa-worktrees/06-repair-agents-g10
bash goals/scripts/check-goal-worktree.sh 06-repair-agents-g10 hermes/06-repair-agents-g10
hermes
```

Submit:

```text
/goal Execute goals/06-repair-agents-g10/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/06-repair-agents-g10 after branch_ready is fully green.
```

## G07 — Exact Patch Apply, G11, and Loop Protection

```bash
cd /home/ubuntu/amfa-worktrees/07-patch-validation-loop
bash goals/scripts/check-goal-worktree.sh 07-patch-validation-loop hermes/07-patch-validation-loop
hermes
```

Submit:

```text
/goal Execute goals/07-patch-validation-loop/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/07-patch-validation-loop after branch_ready is fully green.
```

## G08 — Startup Reconciliation and Migration Assistant

```bash
cd /home/ubuntu/amfa-worktrees/08-reconciliation-assistant
bash goals/scripts/check-goal-worktree.sh 08-reconciliation-assistant hermes/08-reconciliation-assistant
hermes
```

Submit:

```text
/goal Execute goals/08-reconciliation-assistant/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/08-reconciliation-assistant after branch_ready is fully green.
```

## G09 — Final Assurance, Delivery, Reporting, and G13–G15

```bash
cd /home/ubuntu/amfa-worktrees/09-assurance-delivery-report
bash goals/scripts/check-goal-worktree.sh 09-assurance-delivery-report hermes/09-assurance-delivery-report
hermes
```

Submit:

```text
/goal Execute goals/09-assurance-delivery-report/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/09-assurance-delivery-report after branch_ready is fully green.
```

## G10 — Integrated Angular 18→21 Runtime Proof

```bash
cd /home/ubuntu/amfa-worktrees/10-full-runtime-proof
bash goals/scripts/check-goal-worktree.sh 10-full-runtime-proof hermes/10-full-runtime-proof
hermes
```

Submit:

```text
/goal Execute goals/10-full-runtime-proof/GOAL.md completely. Follow root AGENTS.md and every referenced shared, source, task, acceptance, contract, manual, documentation, audit, completion, and push rule. Preserve the distinction between branch_ready and integration_verified. Push only hermes/10-full-runtime-proof after branch_ready is fully green.
```
