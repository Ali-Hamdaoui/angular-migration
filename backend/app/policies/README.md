# Policies

Owns topology support policy, command allowlists, install-script policy,
changed-file sensitivity rules, auto-approval rules, and migration support
levels.

Policies must be explicit and versioned where behavior can affect a run. They
must not be hardcoded in routers, agents, or frontend components, and repository
content must not override backend policy.